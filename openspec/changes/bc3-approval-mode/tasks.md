# Tasks

1. [ ] Add strict mode policy tests and verifier evidence semantics.
2. [ ] Add settings mode validation and pass mode through both verifier constructors.
3. [ ] Persist approval mode in BC3 and catalog snapshots; default legacy rows to `github_review`.
4. [ ] Enforce mode match before apply writes and expose safe responses/contracts.
5. [ ] Add migration 09 and focused unit/integration PostgreSQL validation.
6. [ ] Document the mode and operational invariants.

## Review Workload Forecast
Estimated feature delta: approximately 180–260 changed lines excluding tests and SDD artifacts; target is under 400 lines. No chain is expected.
