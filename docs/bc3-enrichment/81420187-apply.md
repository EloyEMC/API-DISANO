# BC3 apply authorization: 81420187

This change authorizes the production BC3 enrichment operation for product `81420187` after independent GitHub review.

The operation is limited to:

- `bc3_descripcion_corta`
- `bc3_descripcion_larga`

It must preserve:

- `pvp = 190.0`
- `descontinuado = 0`
- all unrelated product fields

The production write must use the API `preview -> approve -> apply` flow with the exact PR head SHA. No direct database write is permitted.

Independent GitHub approval must be recorded before the production approval endpoint is called; merging alone is not sufficient.
